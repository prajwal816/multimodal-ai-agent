# Place test images here
# ────────────────────────
# The agent accepts any common image format: .jpg .png .webp .bmp .gif
#
# Example:
#   python main.py --task "Describe the scene" --image data/sample_images/my_photo.jpg
#
# In stub mode (config.yaml → vision.backend: stub), a rich text description
# is generated from the filename without any ML model or file read.
# This means the agent runs even with a non-existent image path.
